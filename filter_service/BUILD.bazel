load("@rules_cc//cc:defs.bzl", "cc_binary")

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "filter_service",
    srcs = [
        "dynamo_repository.h",
        "filter_service.cc",
        "grpc_server.h",
        "phtree_posts.h",
    ],
    deps = [
        "//protos:cpp_protos",
        "@phtree",
        "@aws-sdk-cpp//:core",
        "@aws-sdk-cpp//:dynamodb",
    ],
)
